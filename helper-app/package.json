{
  "name": "@gmail-glean-reply-drafter/helper-app",
  "version": "0.1.36",
  "private": true,
  "description": "Desktop helper app that runs the local Glean Email Responder backend.",
  "type": "module",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npm run build && electron dist/main.cjs",
    "package:mac": "npm run build -w @gmail-glean-reply-drafter/extension && npm run build && node package-mac.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gmail-glean-reply-drafter/backend": "file:../backend",
    "electron-is-dev": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "electron": "43.1.0",
    "esbuild": "^0.25.3",
    "typescript": "^5.8.3"
  }
}
